import { Meta, Story, Props, Canvas } from "@storybook/addon-docs";
import { withKnobs, boolean, select, text } from "@storybook/addon-knobs";
import { Redirect } from "./Redirect";

<Meta title="Atoms/Redirect" component={Redirect} />

export const Template = (args) => <Redirect {...args} />;

# Enlace

[Código fuente](https://github.com/SGDavid2719/DesignSystem/tree/master/src/stories/atoms/Redirect)

## Tabla de contenidos

-   [Descripción general](#descripción-general)
-   [API Componente](#api-componente)
-   [Desarrollo atómico](#desarrollo-atómico)

## Descripción general

Un enlace se utiliza para saltar entre documentos o entre secciones del mismo.

<Canvas>
    <Story
        name="Default"
        args={{
            href: "/?path=/story/atoms-button--default",
            rel: "next",
            ariaLabel: "children",
            children: <p>Children</p>,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## API Componente

<Props of={Redirect} />

## Desarrollo atómico

Al ser una unidad indivisible se utiliza únicamente la etiqueta HTML propia para los enlaces.

```jsx
<a href aria-label rel>
    {children}
</a>
```
