import { Meta, Story, Props, Canvas } from "@storybook/addon-docs";
import { withKnobs, boolean, select, text } from "@storybook/addon-knobs";
import { Button } from "./Button";

<Meta title="Molecules/ButtonWithIcon" component={Button} />

export const Template = (args) => <Button {...args} />;

# Botón

[Código fuente](https://github.com/SGDavid2719/DesignSystem/tree/master/src/stories/atoms/Button)

## Tabla de contenidos

-   [Descripción general](#descripción-general)
-   [API Componente](#api-componente)
-   [Desarrollo atómico](#desarrollo-atómico)

## Descripción general

Componente formado por un botón y un icono. Este icono debe formar parte de la lista de iconos disponibles.

<Canvas>
    <Story
        name="Default"
        args={{
            label: "Button",
            onClick: () => console.log("Testing button onClick function"),
            renderIcon: "add",
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## API Componente

<Props of={Button} />

## Desarrollo atómico

Esta molécula está compuesta de un botón y un icono.

```jsx
<button>
    <div className={`my-auto`}>
        {<Icon iconId={renderIcon} iconClassName={iconClassName} />}
    </div>
</button>
```
