import { Meta, Story, Props, Canvas } from "@storybook/addon-docs";
import { withKnobs, boolean, select, text } from "@storybook/addon-knobs";
import { Button } from "./Button";

<Meta title="Atoms/Button" component={Button} />

export const Template = (args) => <Button {...args} />;

# Botón

[Código fuente](https://github.com/SGDavid2719/DesignSystem/tree/master/src/stories/atoms/Button)

## Tabla de contenidos

-   [Descripción general](#descripción-general)
-   [API Componente](#api-componente)
-   [Desarrollo atómico](#desarrollo-atómico)

## Descripción general

Un botón es un elemento interactivo que se define con la etiqueta `<button>`, y al cual es necesario añadir atributos como `onClick` para especificar lo que debe ocurrir cuando se hace click.

<Canvas>
    <Story
        name="Default"
        args={{
            label: "Button",
            onClick: () => console.log("Testing button onClick function"),
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## API Componente

<Props of={Button} />

## Desarrollo atómico

Al ser una unidad indivisible se utiliza únicamente la etiqueta HTML propia para los botones.

```jsx
<button />
```
