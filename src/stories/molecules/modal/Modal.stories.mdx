import { Meta, Story, Props, Canvas } from "@storybook/addon-docs";
import { withKnobs, boolean, select, text } from "@storybook/addon-knobs";
import { Modal } from "./Modal";

<Meta title="Molecules/Modal" component={Modal} />

export const Template = (args) => <Modal {...args} />;

# Icono enlace

[Código fuente](https://github.com/SGDavid2719/DesignSystem/tree/master/src/stories/molecules/Modal)

## Tabla de contenidos

-   [Descripción general](#descripción-general)
    -   [Versión lateral](#versión-lateral)
-   [API Componente](#api-componente)
-   [Desarrollo atómico](#desarrollo-atómico)

## Descripción general

Componente ocultable que contiene un título y un cuerpo.

<Canvas>
    <Story
        name="Default"
        args={{
            modalTitle: "Modal Title",
            modalBody: <p className="p-4">Modal body</p>,
            setShowModal: () => {},
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Versión lateral

<Canvas>
    <Story
        name="Left"
        args={{
            modalTitle: "Modal Title",
            modalBody: <p className="p-4">Modal body</p>,
            setShowModal: () => {},
            align: "left",
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## API Componente

<Props of={Modal} />

## Desarrollo atómico

Al ser una molécula, este componente está compuesto por dos o varios átomos. <br />
En este caso, se trata del componente Backdrop para cerrar el modal al hacer click fuera, un encabezado, un botón y un body.

```jsx
<div>
    <Backdrop onOutsideClick />
    <div>
        <div>
            <h1>{modalTitle}</h1>
            <Button label renderIcon align onClick />
        </div>
        <div>{modalBody}</div>
    </div>
</div>
```
